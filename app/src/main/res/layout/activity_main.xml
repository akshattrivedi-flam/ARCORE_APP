<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <io.github.sceneview.ar.ArSceneView
        android:id="@+id/sceneView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.example.arcoreapp.BoundingBoxOverlay
        android:id="@+id/boxOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="#80000000"
        android:padding="8dp"
        android:text="Scan floor to place object"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ScrollView
        android:id="@+id/controlPanelScroll"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:background="#A6000000"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <!-- Scale Controls -->
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Scale (cm)" android:textColor="#FFFFFF" android:textStyle="bold" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlScaleX" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlScaleY" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlScaleZ" />

            <!-- Rotation Controls -->
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Rotation (deg)" android:textColor="#FFFFFF" android:layout_marginTop="8dp" android:textStyle="bold" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlRotX" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlRotY" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlRotZ" />

            <!-- Translation Controls -->
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Translation (cm)" android:textColor="#FFFFFF" android:layout_marginTop="8dp" android:textStyle="bold" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlTransX" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlTransY" />
            <include layout="@layout/layout_control_row" android:id="@+id/ctrlTransZ" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="16dp">

                <Button
                    android:id="@+id/btnRecord"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Start Record"
                    android:backgroundTint="#FF4444" />

                <Button
                    android:id="@+id/btnExport"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Export Zip"
                    android:layout_marginStart="8dp" />

                <Button
                    android:id="@+id/btnReset"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Reset"
                    android:backgroundTint="#808080"
                    android:layout_marginStart="8dp" />
            </LinearLayout>

            <TextView
                android:id="@+id/fpsText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Status: Idle"
                android:textColor="#00FF00"
                android:textSize="12sp"
                android:layout_marginTop="4dp" />

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
